xde-icons -- history of user-visible changes.  2024-05-07

Release xde-icons-1.8 released 2024-05-07
-----------------------------------------

git tag -n100 -l 1.8

1.8             Maintenance Release 1.8

    This is another release of the xde-icons package that provides desktop
    icons for the XDE (X Desktop Environment). It provides the necessary
    icon sets to work with XDE tools as well as supporting the desktop
    entries in the xde-helpers package.

    This release is a maintenance release that updates build system and
    removes some conflicting icons.

    Included in the release is an autoconf tarball for building the package
    from source. See the NEWS and TODO file in the release for more
    information. Please report problems to the issues list on GitHub.

git shortlog -e -n -w80,6,8 1.7...1.8

Brian Bidulock <bidulock@openss7.org> (3):
      post-release update
      updates
      update release files


Release xde-icons-1.7 released 2022-02-03
-----------------------------------------

git tag -n100 -l 1.7

1.7             Maintenance Release 1.7

    This is the seventh release of the xde-icons package that provides
    desktop icons for the XDE (X Desktop Environment). It provides the
    necessary icon sets to work with XDE tools as well as supporting the
    desktop entries in the xde-helpers package.

    This release is a maintenance release that updates build system and
    adds gh-pages.

    Included in the release is an autoconf tarball for building the package
    from source. See the NEWS and TODO file in the release for more
    information. Please report problems to the issues list on GitHub.

git shortlog -e -n -w80,6,8 1.6...1.7

Brian Bidulock <bidulock@openss7.org> (5):
      post-release update
      update build flags
      add gh-pages
      update release files
      update release files


Release xde-icons-1.6 released 2021-05-08
-----------------------------------------

git tag -n100 -l 1.6

1.6             Maintenance Release 1.6

    This is the sixth release of the xde-icons package that provides
    desktop icons for the XDE (X Desktop Environment). It provides the
    necessary icon sets to work with XDE tools as well as supporting the
    desktop entries in the xde-helpers package.

    This release is a maintenance release that removes some unnecessary
    icons that conflict with the packages that utilize them.

    Included in the release is an autoconf tarball for building the package
    from source. See the NEWS and TODO file in the release for more
    information. Please report problems to the issues list on GitHub.

git shortlog -e -n -w80,6,8 1.5...1.6

Brian Bidulock <bidulock@openss7.org> (3):
      post-release update
      remove more conflicting icons
      update release files


Release xde-icons-1.5 released 2020-05-09
-----------------------------------------

git tag -n100 -l 1.5

1.5             Maintenance Release 1.5

    This is the fifth release of the xde-icons package that provides
    desktop icons for the XDE (X Desktop Environment). It provides the
    necessary icon sets to work with XDE tools as well as supporting the
    desktop entries in the xde-helpers package.

    This release is a maintenance release that removes some unnecessary
    icons that conflict with the packages that utilize them.

    Included in the release is an autoconf tarball for building the package
    from source. See the NEWS and TODO file in the release for more
    information. Please report problems to the issues list on GitHub.

git shortlog -e -n -w80,6,8 1.4...1.5

Brian Bidulock <bidulock@openss7.org> (7):
      post-release update
      better cscope and MDOCDATE
      whoops
      remove unnecessary gnumeric kmail kwrite icons
      remove more conflicting icons
      update release notes
      update release files


Release xde-icons-1.4 released 2019-09-03
-----------------------------------------

git tag -n100 -l 1.4

1.4             Maintenance Release 1.4

    This is the fourth release of the xde-icons package that provides
    desktop icons for the XDE (X Desktop Environment). It provides the
    necessary icon sets to work with XDE tools as well as supporting the
    desktop entries in the xde-helpers package.

    This release is a maintenance release that simply provides better
    autoconf directory defaults and improves the build system more.

    Included in the release is an autoconf tarball for building the package
    from source. See the NEWS and TODO file in the release for more
    information. Please report problems to the issues list on GitHub.

git shortlog -e -n -w80,6,8 1.3...1.4

Brian Bidulock <bidulock@openss7.org> (8):
      update release notes
      provide better autoconf dir defaults
      better defaults now
      build release notes with dist
      do not need c compiler
      no c compiler necessary
      add license file to repository
      update release files


Release xde-icons-1.3 released 2019-09-02
-----------------------------------------

git tag -n100 -l 1.3

1.3             Maintenance Release 1.3

    This is the third release of the xde-icons package that provides
    desktop icons for the XDE (X Desktop Environment). It provides the
    necessary icon sets to work with XDE tools as well as supporting the
    desktop entries in the xde-helpers package.

    This release is a maintenance release the simply updates the build
    system for better NEWS generating, handling of annotated tags, and
    converts to lzip compression for generated archives.

    Included in the release is an autoconf tarball for building the package
    from source. See the NEWS and TODO file in the release for more
    information. Please report problems to the issues list on github.

git shortlog -e -n -w80,6,8 1.2...1.3

Brian Bidulock <bidulock@openss7.org> (15):
      git hates trailing space
      better formatting
      limit changelog
      update build system
      reformat TODO
      update for gettext 0.20.1
      generate NEWS better
      perform parallel build
      perform parallel distcheck
      specify man directory
      add release notes
      put filename last in sed command
      update build process
      update build add release notes to build
      update release files


Release xde-icons-1.2 released 2016-07-09
-----------------------------------------

git shortlog -e -n -w80,6,8 1.1...1.2

Brian Bidulock <bidulock@openss7.org> (43):
      reremove conflicting arch-logo files
      add missing start-here
      some adjustments
      add icon for 2bwm
      added a few missing icons
      add icons for nano editor
      added aterm icon
      added fltk-editor icon
      add xv.xpm
      add gv icon
      add eeschema icon
      add icons for imagic and chbg
      add some new window icons for libwnck+
      add two missing window icons
      added two uninstalled pixmaps to repo
      add more wm icons ADWM, DWM and I3
      add xdwm, also limit compressed material
      more wm icons: larswm, yeahwm
      more window manager icons: evilwm, wm2
      add wmx and wmii window manager icons
      more wm icons: pawm, spectrwm, velox, wind, metacity
      added aewm icon
      add in some new icons
      remove some conflicting icons
      remove conflicting dia icons
      add svg for xterm
      remove conflicting file-roller pngs
      add svg icon for awesome into the mix
      do not add unnecessary icons yet
      add AfterStep svg icon
      thicker characters
      rename afterstep icon
      overwrite with svg conversions
      move afterstep icon back to AfterStep
      kicad package now has this
      note librsvg incompatability
      add some category icons for menus
      add category icons to package
      minor updates to build system
      add ignore files
      simplify distribution
      get distcheck working
      update release files

